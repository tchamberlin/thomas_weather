{
  "name": "thomas-weather",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "backfill:history": "node scripts/backfill-history.mjs",
    "upload:history-kv": "node scripts/upload-history-kv.mjs",
    "add-station": "node scripts/add-station.mjs",
    "discover-neighbors": "node scripts/discover-neighbors.mjs",
    "backfill-neighbors": "node scripts/backfill-neighbors.mjs",
    "upload-neighbors-kv": "node scripts/upload-neighbors-kv.mjs",
    "generate:qr": "node scripts/generate-qr-codes.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "start": "wrangler dev --test-scheduled",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "@types/qrcode": "^1.5.6",
    "qrcode": "^1.5.4",
    "typescript": "^5.5.2",
    "vitest": "^4.1.6",
    "wrangler": "^4.90.0"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.21",
    "leaflet": "^1.9.4",
    "uplot": "^1.6.32"
  }
}
